fix(ai): add robust JSON parsing fallback for AI commands

- useChat: Strip markdown codeblocks (e.g. \`\`\`json) inside <EXCEL_CMD> tags.
- useChat: Add fallback parser that replaces unescaped newlines and fixes trailing commas before throwing JSON parse errors.
- prompts: Explicitly instruct models to escape internal double quotes and newlines in string values.
