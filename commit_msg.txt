fix(ai): add NVIDIA NIM endpoint auto-detection to generic provider

- GenericOpenAIProvider: Detect `integrate.api.nvidia.com` in custom base URLs and delegate to NvidiaProvider. This ensures that NVIDIA-specific constraints (like `supportsStreamOptions = false`) are automatically applied even when users set up a custom model pointing to the NIM API.
